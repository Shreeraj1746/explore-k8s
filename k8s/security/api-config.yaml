apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: endpoint-stats
data:
  config.json: |
    {
      "LOG_LEVEL": "INFO",
      "ALLOWED_ORIGINS": "https://api.endpoint-stats.com",
      "METRICS_ENABLED": "true",
      "RATE_LIMIT": "100"
    }
